{
  "name": "@al-infaaq/db",
  "private": true,
  "type": "module",
  "exports": {
    ".": "./src/index.ts",
    "./client": "./src/client.ts",
    "./runtime": "./src/runtime.ts",
    "./schema": "./src/schema.ts",
    "./queries/health": "./src/queries/health.ts"
  },
  "scripts": {
    "build": "tsc --noEmit",
    "db:generate": "prisma generate",
    "db:migrate": "prisma migrate dev",
    "db:migrate:deploy": "prisma migrate deploy",
    "db:studio": "prisma studio",
    "lint": "biome check .",
    "typecheck": "tsc --noEmit"
  },
  "dependencies": {
    "@prisma/adapter-pg": "^7.8.0",
    "@prisma/client": "^7.8.0",
    "pg": "^8.21.0",
    "zod": "^4.0.0"
  },
  "devDependencies": {
    "@al-infaaq/tsconfig": "workspace:*",
    "@types/node": "^20",
    "prisma": "7"
  }
}
